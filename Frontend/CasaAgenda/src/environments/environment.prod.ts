// Este valor se reemplaza al hacer el build de producción (ver angular.json -> fileReplacements).
// Cambiar acá por la URL real del backend deployado antes de hacer `ng build --configuration production`.
export const environment = {
  production: true,
  apiUrl: 'https://TU-BACKEND-DEPLOYADO.example.com',
};
